# Input Data
请基于以下【简历内容】与【岗位 JD】进行岗位匹配分析，并严格按照 `JdMatchResponse` 的 JSON 结构输出结果。

## 候选人简历
---简历内容开始---
{resumeText}
---简历内容结束---

## 岗位 JD
---JD 内容开始---
{jdContent}
---JD 内容结束---

## 分析要求
1. 先从 JD 中识别出核心必备技能、职责要求、业务场景、资历要求、加分项。
2. 再从简历中寻找与 JD 对应的直接证据，包括技术栈、项目经历、职责描述、性能优化、稳定性治理、业务结果等。
3. `matchScore` 只衡量“候选人与 JD 的真实匹配程度”。
4. `overallScore` 在 `matchScore` 基础上，进一步考虑简历是否把价值表达清楚，是否存在“做过但没写明白”的情况。
5. `missingSkills` 只保留 JD 中明确要求、但简历没有足够证据支撑的技能；不要把泛泛的软性要求或无关要求塞进去。
6. `missingSkills.skillLevel` 必须结合 JD 原文语气填写，例如“必须”“熟悉”“掌握”“有经验”“优先”；如果 JD 没写清楚但明显是硬要求，可归纳为“必须”。
7. `suggestions` 重点给出以下几类建议：
   - 简历中已有事实但缺少 JD 关键词表达
   - 项目描述没有体现与 JD 对应的职责和结果
   - 技能项写法过泛，无法支撑目标岗位
   - 应优先补充的量化结果、技术深度、业务价值
8. 所有建议必须基于现有简历事实，不得建议伪造经验。

## 输出要求
请直接输出 JSON 对象，不要包含任何 Markdown 代码块，不要输出字段解释，不要输出额外文本。
输出字段必须严格为：
- `overallScore`
- `matchScore`
- `missingSkills`
- `suggestions`
